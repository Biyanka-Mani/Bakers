<%= form_for([:admin, @user]) do |f| %>
  <div class="field">
     <%= f.label :name %><br />
    <%= f.text_area :name %>
    <%= f.label :email %><br />
    <%= f.email_field :email %>
  </div>

  <div class="actions">
    <%= f.submit 'Create User' %>
  </div>
<% end %>