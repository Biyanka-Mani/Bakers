<!DOCTYPE html>
<html>
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
  </head>
  <body>
    <h1>Order Confirmation</h1>
    <p>Thank you for your order!</p>
    <p>Order ID: <%= @order.id %></p>
    <p>Total Amount: <%= number_to_currency(@order.total_amount) %></p>
    
    <h2>Order Details:</h2>
    <ul>
      <% @products.each do |order_product| %>
        <li>
          <strong>Product:</strong> <%= order_product.product.name %><br>
          <strong>Quantity:</strong> <%= order_product.quantity %><br>
          <strong>Price:</strong> <%= number_to_currency(order_product.product.unit_price) %>
        </li>
      <% end %>
    </ul>

    <p>We will notify you once your order is shipped.</p>
  </body>
</html>
